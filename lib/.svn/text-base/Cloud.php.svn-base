<?php

/**
 * We now support cloud computing
 */
class Cloud {
    
    public static function synergize($top, $right, $scale){
        $cfg = array('top' => $top, 'right' => $right, 'scale' => $scale);
        echo(TemplateParser::parse('cloud.html', $cfg));
    }
    
}